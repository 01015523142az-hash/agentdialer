# Agent dialer

**Generated. Do not edit anything in this repository.**

The source is `dialer/index.html` in the main portal repo, and this copy is
produced by `tools/publish-dialer.py`. Editing here is silently undone by the
next publish.

Serves <https://dialer.proptechnologyai.com> from GitHub Pages, and since the cutover this is the
ONLY place that console is served from -- `staffportal.proptechnologyai.com`
now answers `/dialer/` with a redirect stub, also generated by that script
(`--stubs`).

The only difference from the source is that cross-console links are made
absolute. Root-relative paths (`/`, `/dialer/`) are correct while everything
is on one host and wrong once it is not -- `/` here is this console, not the
staff portal.
